"Beginner-Level Coding Questions"


1. Write a code to read two numbers and pritn their sum

#include<stdio.h>
int main()
{
    int a,b;
    int result;
    printf("Enter the numbers:");
    scanf("%d%d",&a,&b);
    result=a+b;
    printf("result is %d + %d = %d",a,b,result);
    return 0;
}


2. Write a code to read a character and print it back to the user

#include<stdio.h>
int main()
{
    char name;
    printf("enter the character :");
    scanf("%c",&name);
    printf("character : %c",name);
    return 0;
}


3. Write a code to read a float and print it with 2 decimals

#include<stdio.h>
int main()
{
    float a;
    printf("Enter the value :");
    scanf("%f",&a);
    printf("Float value is : %.2f",a);
    return 0;
}


4.Write a program to take your name as input and print a welcome message.

#include<stdio.h>
int main()
{
  char name[100];
  printf("Enter name:");
  scanf("%s",&name);
  printf("Hello %s",name);
 
  return 0;
}


5.Write a program to calculate the area of a rectangle.

#include<stdio.h>
int main()
{
  int a,b,area;
  printf("Enter the length and breadth :");
  scanf("%d%d",&a,&b);
  area=a*b;
  printf("Area is %d",area);
  return 0;
}



"Intermediate-Level Coding Questions"



6.Write a program to swap two numbers using a temporary variable.

#include <stdio.h>

void main() {
    int a,b,c;
    printf("Enter the number of a :");
    scanf("%d",&a);
    printf("Enter the number of b : ");
    scanf("%d",&b);
    c=a;
    a=b;
    b=c;
    printf("result is \n");
    printf("a= %d\n",a);
    printf("b= %d",b);
    
}


7. Write a program to calculate the simple interest.

#include <stdio.h>

void main() {
    int p,t,r;
    float interest;
    printf("enter the values of p,t,r :");
    scanf("%d%d%d",&p,&t,&r);
    interest=p*t*r/100;
    printf("simple interest is : %f",interest);
    
}


8.Write a program to convert temperature from Celsius to Fahrenheit.

#include <stdio.h>

void main() {
    float f,c;
    printf("enter the value of c :");
    scanf("%f",&c);
    f=(c*9/5)+32;
    printf("the value of celsius in fahrenheit is : %f",f);

}


9.Write a program to read a 3-digit number and print the sum of its digits.

#include <stdio.h>

void main() {
    int number,a,b,c,sum;
    printf("Enter the three digit number :");
    scanf("%d",&number);
    a=number/100;
    c=number%10;
    b=(number/10)%10;
    sum=a+b+c;
    printf("result is : %d",sum);
}


10.Write a program to check whether a number is even or odd.

